spring:
  datasource:
    url: jdbc:mysql://localhost:3306/university_booking_db?useSSL=false&serverTimezone=UTC
    username: springstudent
    password: Mahmoud@088564
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jwt:
    secret: dev-secret-key-123456
    expiration: 3600000
